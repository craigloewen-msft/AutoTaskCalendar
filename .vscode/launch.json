{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Launch Edge against localhost",
            // Port 8080 is the "default" instance. If you set AUTOTASKCALENDAR_INSTANCE,
            // run `npm run db:status` to find your web port and update this URL.
            "url": "http://localhost:8080",
            "webRoot": "${workspaceFolder}/webinterface/src"
        },
        {
            "type": "node",
            "request": "attach",
            "name": "Node: Nodemon",
            "processId": "${command:PickProcess}",
            "restart": true,
            "protocol": "inspector"
        }
    ],
    "compounds": [
        {
            "name": "Node and Browser",
            "configurations": ["Node: Nodemon", "Launch Edge against localhost"]
        }
    ]
}
