# Local config, overrides defaultconfig.js
config.js

node_modules/
dist/
nohup.out
/.phoenix

# Editor settings are personal, but the shared debug config is committed.
.vscode/*
!.vscode/launch.json
